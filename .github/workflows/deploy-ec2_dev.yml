name: Development

on:
    push:
        branches:
            - dev

jobs:
    call-deploy:
        name: Deploy to dev
        uses: ./.github/workflows/build-deploy.yml
        with:
            environment: frontierconsolidated.com
            hostname: dev.frontierconsolidated.com
            service: frcn-dev-backend.service
        secrets: inherit
